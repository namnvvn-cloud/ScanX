# === Supabase Postgres ===
# Project Settings → Database → Connection string → chọn "URI", chế độ Session hoặc Transaction pooler.
DATABASE_URL="postgresql://postgres:PASSWORD@db.xxxxxxxxxxxx.supabase.co:5432/postgres"

# === Firebase Admin SDK ===
# Firebase Console → Project Settings → Service accounts → Generate new private key
# Tải file .json về, mở ra, dán NGUYÊN VĂN nội dung (dạng JSON 1 dòng) vào giữa 2 dấu nháy đơn.
FIREBASE_SERVICE_ACCOUNT_JSON='{"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}'

# === Cloudflare R2 ===
# Cloudflare Dashboard → R2 → Manage API Tokens → Create API Token (quyền Object Read & Write)
R2_ENDPOINT="https://<ACCOUNT_ID>.r2.cloudflarestorage.com"
R2_BUCKET="scanx-documents"
R2_ACCESS_KEY_ID="..."
R2_SECRET_ACCESS_KEY="..."

# === Server ===
PORT=3000
